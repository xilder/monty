#include "monty.h"
/**
 * f_queue - converts to a queue
 * @head: beginning of the list
 * @counter: line counter
 */
void f_queue(stack_t **head, unsigned int counter)
{
	(void) head;
	(void) counter;
	drive.sq = 1;
}
