#include "monty.h"
/**
 * f_stack - converts to stack
 * @head: beginning of the list
 * @counter: line counter
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void) head;
	(void) counter;
	drive.sq = 0;
}
